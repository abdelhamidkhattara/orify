# Orify — secrets only (never commit .env.local)

# Required on Vercel (production):
DATABASE_AUTH_TOKEN=
SESSION_SECRET=
OWNER_ENCRYPTION_KEY=
BLOB_READ_WRITE_TOKEN=

# Hardcoded in src/lib/config.ts (not env):
# OWNER_PASSWORD=owner123
# DEMO_PASSWORD=jarir
# PROD_APP_URL / PROD_QR_BASE_URL = https://orify.vercel.app
# TURSO_DATABASE_URL = libsql://orify-hamidkhattara.aws-eu-west-1.turso.io

# Optional local: force Turso instead of file DB while developing
# DATABASE_URL=libsql://orify-hamidkhattara.aws-eu-west-1.turso.io
# DATABASE_AUTH_TOKEN=...
